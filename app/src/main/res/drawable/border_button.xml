<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/>
    <stroke
        android:width="2dp"
        android:color="@color/black" /> <!-- ganti dengan warna utama aplikasi -->
    <corners android:radius="8dp" />
</shape>
